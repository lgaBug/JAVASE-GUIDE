<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
        "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="com.lga.mybatis.dao.UserDao" >

    <cache ></cache>

    <resultMap id="baseMap" type="com.lga.mybatis.vo.User">
        <id column="id" property="id"></id>
        <result column="name" property="name"></result>
        <result column="password" property="password"></result>
        <result column="age" property="age"></result>
        <association property="score" resultMap="baseScore"></association>
    </resultMap>

    <resultMap id="baseScore" type="com.lga.mybatis.vo.Score">
        <result column="id" property="id"></result>
        <result column="math" property="math"></result>
        <result column="english" property="english"></result>
    </resultMap>

    <select id="findUserById" resultType="com.lga.mybatis.vo.User">
        select * from user where id = #{id}
    </select>

    <select id="findAllUsers" resultType="com.lga.mybatis.vo.User">
        select * from user
    </select>


    <select id="find" resultType="com.lga.mybatis.vo.User" resultMap="baseMap">
        select * from user u1
        left join score s1
        on u1.id = s1.id
        where u1.id = #{arg0}
    </select>

</mapper>